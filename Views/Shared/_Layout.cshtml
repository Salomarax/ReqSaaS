@* Views/Shared/_Layout.cshtml *@
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - ReqSaaS</title>

    <!-- CSS: Bootstrap primero, luego tus estilos -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <!-- Fuente (opcional, usada por site.css) -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />

    @RenderSection("Styles", required: false)
</head>
<body>
    @{
        // Default: true (muestra la barra si la vista no define nada)
        var UseSideBar = ViewData["UseSideBar"] as bool? ?? true;
    }

    @if (UseSideBar)
    {
        <!-- MODO SIDEBAR (app) -->
        <div class="app-shell">
            @await Html.PartialAsync("_SideBar")
            <main class="content">
                @RenderBody()
            </main>
        </div>
    }
    else
    {
        <!-- MODO PLANO (login/landing, sin sidebar) -->
        <div class="plain-shell">
            <main class="plain-content">
                @RenderBody()
            </main>
        </div>
    }

    <!-- Scripts globales -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
